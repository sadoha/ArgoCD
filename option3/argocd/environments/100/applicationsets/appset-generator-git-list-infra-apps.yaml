apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: shared-100-applicationset
  namespace: argocd
  finalizers:
  - resources-finalizer.argocd.argoproj.io
  labels:
    name: shared-100-applicationset
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
    - matrix:
        generators:
        - git:
            repoURL: https://github.com/sadoha/ArgoCD.git
            revision: HEAD
            files:
            - path: option3/argocd/environments/100/components/key-components-infra-apps.yaml
        - list:
            elementsYaml: "{{ .key.components | toJson }}" 
  template:
    metadata:
      namespace: argocd
      name: 'shared-100-infra'
      #name: '{{.name}}'
    spec:
      project: '{{.project}}'
      sources:
      - chart: mongodb
        repoURL: registry-1.docker.io/bitnamicharts  # note: the oci:// syntax is not included.
        targetRevision: 16.3.0
      - chart: etcd
        repoURL: registry-1.docker.io/bitnamicharts  # note: the oci:// syntax is not included.
        targetRevision: 10.5.3

      #- repoURL: 'https://charts.bitnami.com/bitnami'
      #  targetRevision: '10.5.3'
      #  chart: 'bitnami/etcd'
      #- repoURL: '{{.repoUrl}}'
      #  targetRevision: '{{.chartVersion}}'
      #  path: '{{.chart}}'
      #  helm:
      #    values: |
      #      image:
      #        tag: '{{.appVersion}}'
      #    valueFiles:
      #    - $values/option3/{{.product}}/environments/{{.environment}}/apps/{{.appName}}/values.yaml
      #- repoURL: 'https://github.com/sadoha/ArgoCD.git'
      #  targetRevision: HEAD
      #  ref: values
      destination:
        server: https://kubernetes.default.svc
        namespace: 'shared-{{.environment}}-infra'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
        managedNamespaceMetadata: 
          labels:
            project: 'shared-{{.environment}}-infra'
          annotations:
            annotations: 'shared-{{.environment}}-infra'
