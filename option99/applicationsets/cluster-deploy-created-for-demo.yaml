---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: silo-99-cluster-deploy
  namespace: argocd
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  generators:
    - git:
        repoURL: https://github.com/sadoha/ArgoCD.git
        revision: stan-sandbox
        directories:
          - path: option6/environments/*
          - path: option6/environments/*/variables
            exclude: true 
  template:
    metadata:
      namespace: argocd
      name: silo-99-{{path.basename}}-all-applications
    spec:
      project: silo-99
      sources:
      - repoURL: https://github.com/sadoha/ArgoCD.git
        targetRevision: stan-sandbox
        path: option6/environments/{{path.basename}}/applicationsets
      destination:
        namespace: argocd
        server: https://kubernetes.default.svc
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
